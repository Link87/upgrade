export class Offer {
    // tslint:disable-next-line:max-line-length
    constructor(public readonly name: string, public readonly subject: string, public readonly loan: number, public readonly type: string, public readonly description: string) {
    }
}
